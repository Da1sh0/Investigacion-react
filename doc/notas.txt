Instalacion
    1. npm install -g npm
    2. node -v / npm -v
    3. npx create-react-app .
    4. npx create-react-app nombre
    5. npm start


React
    1. Componente es una funcion de js  que retorna html
    2. Componentes siempre en mayusculas
    3. NO se pueden retornar ams de un div por componentes en react

JavaScript
    1.Las funciones flechas son para acortar el codigo de una funcion clasica.
        1.1. Se recomienda usar const name = () =>{Logica}
    2.Los operadoes ternarios son para acortar los if/else
    3.Los objetos const {nombre,edad} usu1
        3.1. Console lo llamas
        3.2. const usu2 = {...usu1} para clonar el usuario1, pero se puede modificar.
    4.Los array let nombre = [name1. name2, name3]
        4.1. Para clonar el array se usa de igual manera que los objetos
        4.2. Pero no bse acambia nada si no que se agrega otro espacio al final.
        4.3. Recordar que un array empieza en 0
